<div class="modal fade" id="importModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <%= form_tag(import_saases_path, multipart: true) do %>
        <div class="modal-header">
          <h5 class="modal-title">SaaS CSVインポート</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <p class="text-muted small">CSVフォーマット: name, category, url, admin_url, description, status</p>
          <div class="mb-3">
            <label class="form-label">CSVファイル</label>
            <%= file_field_tag :file, accept: ".csv", class: "form-control" %>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">キャンセル</button>
          <button type="submit" class="btn btn-primary">
            <i class="bi bi-upload me-1"></i>インポート
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>
