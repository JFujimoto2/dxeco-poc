<h2><%= @task.title %></h2>

<p>以下のタスクが割り当てられました。対応をお願いします。</p>

<table border="1" cellpadding="8" cellspacing="0" style="border-collapse: collapse;">
  <thead>
    <tr>
      <th>対応内容</th>
      <th>対象SaaS</th>
      <th>担当者</th>
    </tr>
  </thead>
  <tbody>
    <% @task_items.each do |item| %>
      <tr>
        <td><%= item.description %></td>
        <td><%= item.saas&.name || "-" %></td>
        <td><%= item.assignee&.display_name || "-" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <strong>対象者:</strong> <%= @task.target_user.display_name %><br>
  <strong>期限:</strong> <%= @task.due_date&.strftime("%Y/%m/%d") || "未設定" %>
</p>

<p><%= link_to "タスクを確認する", task_url(@task) %></p>
