# SaaS管理ツール インフラ構成

---

## 1. 概要

POCは個人サブスクリプションで構築・実証済み。本番では会社テナントに移行し、セキュリティ強化を行う。

| 項目 | POC（現状） | 本番 |
| --- | --- | --- |
| Azureテナント | 個人 | **会社テナント（Entra IDと同一）** |
| サブスクリプション | 個人 | **会社サブスクリプション** |
| 環境数 | 1（dev兼デモ） | **1（prod のみ）** |
| セキュリティ | 最低限 | **VNet統合 + Key Vault + 監視** |
| コンテナ | min 0（手動起動） | **min 0 + CRONで月次バッチ自動実行** |
| DB | 手動で起動/停止 | **常時起動** |
| メール | 未設定 | **Microsoft Graph API（E5に含まれる）** |
| 開発環境 | Azure上 | **ローカル Docker Compose** |

### Azure選択理由

Entra IDと同一テナントで管理でき、認証との親和性が最も高いAzureを選択。
App Service は Ruby ビルトインサポートが終了（2023年4月）しているため、**Container Apps（従量課金）** を採用。

---

## 2. 構成図

```
[ユーザー]
  ↓ HTTPS（Container Apps マネージド証明書）
[Azure Container Apps] ── Rails 8 Docker コンテナ
  │  ├─ Thruster（HTTP/2 プロキシ, port 3000）
  │  ├─ Puma（Web サーバー, port 3001）
  │  └─ Solid Queue（バックグラウンドジョブ、Puma プラグイン）
  ↓
[Azure Database for PostgreSQL Flexible Server]
  ├─ primary: dxceco_poc_production
  ├─ cache:   dxceco_poc_production_cache  (Solid Cache)
  ├─ queue:   dxceco_poc_production_queue  (Solid Queue)
  └─ cable:   dxceco_poc_production_cable  (Solid Cable)

[Azure Container Registry] ── Docker イメージ保管

外部API:
  → Microsoft Graph API（Entra ID認証・ユーザー同期）
  → Power Automate Workflows Webhook（Teams通知）
```

---

## 3. 本番環境リソース

| # | リソース | サービス | スペック | 月額目安 |
| --- | --- | --- | --- | --- |
| 1 | リソースグループ | Resource Group | `rg-saas-mgmt-prod` | ¥0 |
| 2 | アプリケーション | Container Apps (Consumption) | 0.5 vCPU / 1 GiB RAM, min 0〜max 3 | ~¥100-750 |
| 3 | データベース | PostgreSQL Flexible Server | Burstable B1ms（1 vCore / 2 GiB RAM / 32 GB SSD）常時起動 | ~¥3,750 |
| 4 | コンテナレジストリ | Container Registry (Basic) | 10 GB ストレージ | ~¥750 |
| 5 | シークレット管理 | Key Vault (Standard) | シークレット参照（月数百回程度） | ほぼ¥0 |
| 6 | ネットワーク | VNet + サブネット | Container Apps ↔ PostgreSQL 間の閉域接続 | ¥0 |
| 7 | 監視 | Azure Monitor + Log Analytics | ログ収集・アラート（5GB/月まで無料枠） | ほぼ¥0 |
| 8 | SSL証明書 | Container Apps マネージド証明書 | 自動更新 | ¥0 |
| 9 | メール送信 | Microsoft Graph API | E5に含まれる（社内宛のみ） | ¥0 |
| | **合計** | | | **約¥4,650〜6,000** |

> コストの95%以上はPostgreSQL（¥3,750）とACR（¥750）の固定費。Container Appsの従量課金は月1回の利用では誤差レベル。

### コスト削減の判断

| 施策 | 効果 | 採否 |
| --- | --- | --- |
| Container Apps min 0運用 | コンテナ従量課金がほぼゼロ | **採用** |
| DB起動/停止の自動化 | 月¥3,000の節約 | **不採用**（手動アクセス時にDB接続エラーになるリスクが見合わない） |
| dev環境の廃止 | 月¥6,000〜の削減 | **採用**（ローカルDocker Composeで代替） |
| Microsoft Graph APIでメール送信 | メール送信の追加コストゼロ | **採用**（E5に含まれる。Security Defaults有効でもSMTP不要） |
| CRONスケーリングで月次バッチ | 常時起動不要で月次自動実行を両立 | **採用** |

---

## 4. コスト比較

### 外部SaaS vs 自社開発

| 期間 | 自社開発 | 外部SaaS（500名・オプション込み） | 削減額 |
| --- | --- | --- | --- |
| 月額 | **約¥4,700** | 約¥170,000 | — |
| 年額 | **約¥56,000** | 約¥2,040,000 | **約198万円** |
| 3年間 | **約¥170,000** | 約¥6,120,000 | **約595万円** |
| **5年間** | **約¥280,000** | **約¥10,200,000** | **約990万円** |

> ※ 外部SaaSは監査ログ＋IP制限オプション込み（年204万円）
> ※ 自社開発の保守工数（セキュリティパッチ等）は年数日程度の想定

---

## 5. POCからの変更点（セキュリティ強化）

| 項目 | POC | 本番 |
| --- | --- | --- |
| ネットワーク | パブリックアクセス | **VNet統合**（Container Apps ↔ PostgreSQL を閉域接続） |
| DB接続 | ファイアウォール（IP制限） | **VNet内のプライベートアクセス** |
| シークレット管理 | Container Apps 環境変数 | **Key Vault** で一元管理 |
| 監視 | Azure Monitor（基本） | **Log Analytics** でログ集約 + アラートルール |
| レプリカ | min 0（手動） | **min 0 + CRONスケーリング**（月次バッチ時のみ自動起動） |
| 認証 | Entra ID SSO（個人テナント） | 同左（**会社テナントで設定が簡素化**） |

---

## 6. Entra ID（会社テナント移行）

会社テナントに移行することで以下が改善される。

| 項目 | 改善内容 |
| --- | --- |
| SSOログイン | 会社のEntra IDアカウントでそのままログイン（テナント間の信頼設定不要） |
| ユーザー同期 | Graph APIで会社のユーザー情報を直接取得（退職者検出も即時反映） |
| 条件付きアクセス | 会社のMFAポリシーがそのまま適用される |
| アプリ登録 | 会社テナントに登録 →「この組織のみ」で安全 |

### 必要なAPI権限

| 権限 | 種類 | 用途 |
| --- | --- | --- |
| `User.Read.All` | アプリケーション | ユーザー一覧取得・退職者検出 |
| `GroupMember.Read.All` | アプリケーション | グループメンバー同期 |
| `Application.Read.All` | アプリケーション | エンタープライズアプリ（SaaS）一覧取得 |
| `Mail.Send` | アプリケーション | Graph API経由のメール送信 |

> ※ アプリケーション権限のため、**Entra ID管理者による同意（Admin Consent）が必要**

### ユーザー/グループのアクセス管理

アプリにアクセスできるユーザーを制御するには、**エンタープライズ アプリケーション**（「アプリの登録」ではない）から設定する。

| 設定 | 場所 | 内容 |
| --- | --- | --- |
| ユーザー/グループの割り当て | Entra ID → エンタープライズ アプリケーション → 対象アプリ → ユーザーとグループ | 特定のユーザーやグループにアクセスを許可 |
| 割り当て必須の切り替え | 同上 → プロパティ → 「ユーザーの割り当てが必要ですか？」 | **はい**: 割り当てられたユーザー/グループのみログイン可。**いいえ**（デフォルト）: テナント内の全ユーザーがログイン可 |

> **注意:** 「アプリの登録」は API権限・シークレット・リダイレクトURI 等の技術設定用。ユーザーのアクセス管理は「エンタープライズ アプリケーション」で行う。

---

## 7. 技術構成

### Docker イメージ

既存の `Dockerfile` を使用（Rails 8 標準のマルチステージビルド）:

- ベースイメージ: `ruby:3.3.2-slim`
- jemalloc 有効（メモリ最適化）
- Thruster（HTTP/2 プロキシ / アセット圧縮・キャッシュ）
- 非root ユーザー実行
- `bin/docker-entrypoint` で起動時に `db:prepare` 自動実行

### ポート構成

| プロセス | ポート | 環境変数 |
| --- | --- | --- |
| Thruster（外部受付） | 3000 | `HTTP_PORT=3000` |
| Puma（Rails） | 3001 | `PORT=3001`, `TARGET_PORT=3001` |

Container Apps の ingress は port 3000 に接続し、Thruster が Puma (3001) にプロキシする。

### Solid Queue

別コンテナは立てず、Puma プラグインとして同一プロセス内で実行（`SOLID_QUEUE_IN_PUMA=true`）。
レプリカ1つでWeb + ジョブの両方を処理。

### データベース

| DB名 | 用途 |
| --- | --- |
| `dxceco_poc_production` | アプリケーションデータ |
| `dxceco_poc_production_cache` | Solid Cache（Rails キャッシュ） |
| `dxceco_poc_production_queue` | Solid Queue（ジョブキュー） |
| `dxceco_poc_production_cable` | Solid Cable（Action Cable） |

`DATABASE_URL` 環境変数でプライマリDBに接続し、cache/queue/cable は同一サーバー上の別DBとして設定。

---

## 8. CI/CD（GitHub Actions）

### ブランチ戦略

```
feature/xxx ──PR──▶ main ──PR──▶ prod ──自動デプロイ──▶ Azure
```

- `main`: 開発統合ブランチ（PRマージのみ、直接push不可）
- `prod`: 本番リリースブランチ（PRマージで自動デプロイ）
- 両ブランチとも CI 全5ジョブ通過必須

### CI（`ci.yml` — main への push/PR 時）

5ジョブ: lint / scan_ruby / scan_js / test / e2e

### CD（`deploy.yml` — prod への push 時）

```
Azure ログイン → ACR ログイン → Docker ビルド & プッシュ → Container Apps 更新
```

所要時間: 約2分30秒

### GitHub Secrets

| Secret 名 | 用途 |
| --- | --- |
| `AZURE_CLIENT_ID` | サービスプリンシパルのクライアントID |
| `AZURE_CLIENT_SECRET` | サービスプリンシパルのシークレット |
| `AZURE_TENANT_ID` | Azure テナントID |
| `AZURE_SUBSCRIPTION_ID` | Azure サブスクリプションID |
| `ACR_LOGIN_SERVER` | ACR ログインサーバー |
| `ACR_NAME` | ACR 名 |

---

## 9. 環境変数

### 必須

| 変数名 | 用途 |
| --- | --- |
| `RAILS_ENV` | Rails 環境（`production`） |
| `RAILS_MASTER_KEY` | credentials 復号キー |
| `DATABASE_URL` | PostgreSQL 接続URL |
| `DXCECO_POC_DATABASE_PASSWORD` | DB パスワード（cache/queue/cable用） |
| `DB_HOST` | DB ホスト名（cache/queue/cable用） |
| `DB_USERNAME` | DB ユーザー名（cache/queue/cable用） |
| `APP_URL` | アプリの公開URL |
| `ENTRA_CLIENT_ID` | Entra ID クライアントID |
| `ENTRA_CLIENT_SECRET` | Entra ID シークレット |
| `ENTRA_TENANT_ID` | テナントID |
| `SOLID_QUEUE_IN_PUMA` | Puma 内で Solid Queue 起動（`true`） |
| `HTTP_PORT` | Thruster リッスンポート（`3000`） |
| `TARGET_PORT` | Thruster → Puma プロキシ先（`3001`） |
| `PORT` | Puma リッスンポート（`3001`） |

### オプション

| 変数名 | 用途 | デフォルト |
| --- | --- | --- |
| `TEAMS_WEBHOOK_URL` | Teams 通知 Webhook | 未設定でスキップ |
| `TEAMS_WEBHOOK_SURVEY_URL` | サーベイ専用 Webhook | `TEAMS_WEBHOOK_URL` |
| `MAILER_FROM` | 送信元アドレス | - |
| `RAILS_LOG_LEVEL` | ログレベル | `info` |

---

## 10. 運用

### コンテナ（min 0運用）

- 普段は停止（min 0）。**毎月1日 6:00にCRONスケーリングでレプリカを1に起動**し、月次バッチを実行。完了後、アクセスがなければ自動停止
- 管理者がアクセスすれば自動起動（コールドスタート: 数十秒〜1分）
- DBは常時起動（月¥3,750固定）

### 月次バッチ（毎月1日 自動実行）

| バッチ | 内容 |
| --- | --- |
| Entra IDユーザー同期 | 退職者検出の前提処理 |
| 退職者アカウント検出 | 残存アカウントの通知 |
| 契約更新チェック | 30日以内の契約更新を通知 |
| SaaSアカウント同期 | Entra IDエンタープライズアプリからの同期 |

### デプロイ

```bash
# 通常: prod ブランチへの PR マージで自動デプロイ
gh pr create --base prod --head main --title "Release: xxx"

# 手動デプロイ
az acr login --name acrdxcecopoc
docker build -t acrdxcecopoc.azurecr.io/dxceco-poc:latest .
docker push acrdxcecopoc.azurecr.io/dxceco-poc:latest
az containerapp update --name app-dxceco-poc --resource-group rg-dxceco-poc \
  --image acrdxcecopoc.azurecr.io/dxceco-poc:latest
```

### ログ確認・Railsコンソール

```bash
# ログ
az containerapp logs show --name app-dxceco-poc --resource-group rg-dxceco-poc --follow

# Railsコンソール
az containerapp exec --name app-dxceco-poc --resource-group rg-dxceco-poc \
  --command "bin/rails console"
```

---

## 11. POCデプロイ済みリソース

| リソース | 名前 | リソースグループ |
| --- | --- | --- |
| Container App | `app-dxceco-poc` | `rg-dxceco-poc` |
| Container Apps Environment | `cae-dxceco-poc` | `rg-dxceco-poc` |
| PostgreSQL Flexible Server | `psql-dxceco-poc` | `rg-dxceco-poc` |
| Container Registry | `acrdxcecopoc` | `rg-dxceco-poc` |

### POC環境の扱い

会社テナントにprod環境を構築し、動作確認が完了した時点でPOC環境（個人サブスクリプション）は廃止する。

---

## 12. 社内確認事項

### Azure環境

| # | 確認事項 | 確認先 |
| --- | --- | --- |
| 1 | 会社のAzureサブスクリプションの有無 | Azure管理者 / 情シス |
| 2 | サブスクリプションがない場合の新規作成手続き | 経理 / 情シス |
| 3 | リソース作成の権限（共同作成者 or カスタムロール） | Azure管理者 |
| 4 | リソースグループの命名規則 | Azure管理者 |
| 5 | 月額約5,000〜6,000円の予算承認 | 経理 / 上長 |

### Entra ID

| # | 確認事項 | 確認先 |
| --- | --- | --- |
| 6 | アプリ登録の権限 | Entra ID管理者 |
| 7 | API権限のAdmin Consentの承認者 | Entra ID管理者 |
| 8 | サービスプリンシパル作成の可否 | Entra ID管理者 |

### ネットワーク・セキュリティ

| # | 確認事項 | 確認先 |
| --- | --- | --- |
| 9 | VNet設計基準（アドレス空間・サブネット設計） | ネットワーク管理者 |
| 10 | カスタムドメインの使用可否 | インフラ担当 |
| 11 | 社内からのアクセス制限の要否 | セキュリティ担当 |

---

## 13. 構築の進め方

| ステップ | 内容 | 所要時間 |
| --- | --- | --- |
| 1. 確認・承認 | 上記12章の確認事項をクリア、予算承認 | 1〜2週間（社内手続き次第） |
| 2. インフラ構築 | VNet → PostgreSQL → ACR → Container Apps → Key Vault → Monitor | 1日 |
| 3. Entra ID設定 | アプリ登録 → API権限 → Admin Consent → サービスプリンシパル | 0.5日 |
| 4. CI/CD設定 | GitHub Secrets更新 → デプロイワークフロー調整 → テストデプロイ | 0.5日 |
| 5. データ移行・動作確認 | 初期データ投入 → 全機能テスト | 1〜2日 |
| **技術作業の合計** | | **約3〜4日** |

> ※ 社内の承認手続きは組織によって1週間〜数週間かかる可能性あり

---

作成日: 2026年2月10日
更新日: 2026年3月11日（Azureリソース・コスト一覧をマージ、本番構成を反映）
